// GENERATED -- do not hand-edit; regenerate with tools/gen_plain.py
//
// One plain-language description per page slug. layout.js renders it
// beside the page's own technical blurb; the top-bar switch chooses.
export const PLAIN = {
  "activations": "The little non-linear function (GeLU, SiLU\u2026) inside each layer. Without it, stacking layers would collapse into one boring linear step and the model couldn't learn anything interesting. The page lines the common ones up side by side.",
  "ar-vs-diffusion-images": "There are two competing ways to generate a picture, and it is not settled which is better. One writes the image out piece by piece the way a model writes text; the other starts from noise and sharpens the whole canvas together.",
  "attention-patterns": "Letting every word see every other word is expensive, so real models use patterns \u2014 sliding windows, \"sink\" tokens \u2014 to read long documents cheaply (how a model skims a 100-page PDF without melting the GPU).",
  "attention-sinks": "Why the very first word of a conversation quietly matters far more than its content. Models need somewhere to put attention they do not want to spend, and the opening token becomes that parking space \u2014 delete it to save memory and the model falls apart.",
  "audio-tokens": "How a model can generate speech and music at all, given that sound is a wave rather than words. It is converted into a stack of codes, coarse first and refined afterwards, so the fine detail can be dropped without the whole thing falling apart.",
  "backprop": "Every other page shows a model using what it knows; this one shows it learning. It guesses, measures how wrong it was, then works out how to nudge each of its numbers to be slightly less wrong. Switch on descent and watch the error fall to zero \u2014 that loop is all training is.",
  "batch-invariance": "Why an identical prompt can give a different answer even with everything set to be repeatable. Computers add long lists of numbers in whatever order is fastest, that order depends on how many requests are being handled at once, and the tiny differences can change a word.",
  "batchnorm": "A normalization for image networks that re-centers each channel to keep training fast and stable, and behaves differently in training vs. when you actually use it. The quiet workhorse of older vision models.",
  "causal-mask": "A language model writes left-to-right and must not peek at future words while learning. The mask enforces that, which is what lets a model train on whole books at once yet still generate one word at a time.",
  "chunked-prefill": "What stops a single enormous prompt from stalling every other user. Processing it in slices, interleaved with everyone else's next word, costs that one request a little extra time and saves everyone else a lot \u2014 a trade a service usually wants to make.",
  "constrained-decoding": "Why an app can promise that a model's answer will always be valid JSON. It is not persuasion \u2014 at every step the words that would break the format are made impossible to pick, and long stretches that could only go one way are filled in without asking the model at all.",
  "context-editing": "How an assistant that has been working for hours keeps from filling up. It throws away old tool output it no longer needs \u2014 but that editing has a hidden price, and sometimes keeping everything is genuinely cheaper than tidying up.",
  "context-extension": "How a model trained on short documents is stretched to read long ones. Position is encoded as rotation, and the methods differ in which rotations they squeeze \u2014 all of them trading some ability to tell nearby words apart.",
  "context-parallelism": "A choice that only shows up once a model is split across chips: move the stored notes to the questions, or the questions to the notes. Which is cheaper flips depending on whether the model is reading your prompt or writing its reply.",
  "continuous-batching": "The scheduling change that made serving many users at once practical. Waiting for a whole batch to finish means everyone waits for the slowest; refilling each slot the instant it frees keeps the hardware busy and the queue short \u2014 without making any single answer arrive faster.",
  "convolution": "The basic operation of image AI: a small filter slides over a picture, multiplying-and-summing to detect a pattern (an edge, a color). Stack these and you get image recognition. Stride / padding / dilation are the dials for resolution and reach.",
  "delta-rule-gates": "How the newest linear-attention models decide what to forget. One forgetting rate for the whole memory is crude \u2014 you cannot keep one fact while dropping another. Giving every feature its own rate, then separating erasing from writing, is where this family is heading.",
  "depthwise-separable": "A way to do the same convolution far more cheaply, by splitting it into two simpler steps: filter each channel on its own, then mix the channels together. It is the trick behind MobileNet and most of the vision that runs on a phone rather than in a data centre.",
  "diffusion-noise": "How an image generator is taught. You take a real picture, corrupt it by a known amount, and train a model to say what was added \u2014 do that at every level of corruption and you can start from pure static and walk backwards to an image.",
  "diffusion-sampler": "What the \u2018steps\u2019 slider in an image generator actually does. It is not a quality setting the model was trained with \u2014 it is how many stops you make along a path. Fewer stops is faster and cruder; past a point, more stops buys nothing.",
  "disaggregation": "Why large deployments sometimes use two different sets of machines for one model. Digesting your prompt is a burst of raw computation; writing the reply is a slow trickle limited by memory speed. Splitting them lets each be built for its own job \u2014 at a shipping cost.",
  "dit": "The architecture behind current image and video generators. It is the same kind of network that powers chatbots, with one clever difference in how your prompt reaches it, and that difference is what let these models keep getting bigger.",
  "dot-product": "The dot product measures how much two lists of numbers point the same way \u2014 a model's basic \"how similar are these two things?\" It's the atom inside matmul and attention, and it's exactly the \"cosine similarity\" that powers search and recommendation engines.",
  "dtype-bits": "A model\u2019s numbers can be stored with more or fewer bits, and that trade is the whole idea behind quantized models that run on a phone. It also shows the counter-intuitive part: a 4-bit model is not 4 bits per number, but 4 bits plus one shared multiplier for every group of them.",
  "dual-batch-overlap": "Chips waste a surprising amount of time waiting for data to arrive from other chips. Running two halves of the job out of step lets one work while the other waits \u2014 though cut the job too finely and you lose more than you gain.",
  "embedding": "Computers only do math, so each word/token is first looked up as a row of numbers (its \"embedding\"). This is the very first thing that happens to your prompt, and the same table is often reused to produce the output.",
  "expert-placement": "Large models are split across many chips, and each chip holds a few specialist sub-networks. Which specialist sits where turns out to matter as much as which one gets picked, because everything else waits on whichever chip is busiest.",
  "feature-maps": "What a CNN actually sees at each depth: early layers find edges, middle layers textures, deep layers parts and objects. This makes the famous edges \u2192 textures \u2192 parts story visible \u2014 the clearest intuition for \"what is the network learning?\"",
  "few-step-distillation": "How image generators got fast enough to update as you type. A second model is taught to leap over most of the work the first one did step by step, and the cost is real: its results start to look like each other, so you get speed and lose variety.",
  "flash-attention": "A clever way to compute attention without ever building the giant word-by-word comparison table in memory \u2014 it streams through in tiles. This single optimization is what makes long-context models practical; it's in nearly every modern stack.",
  "forward-pass": "The whole thing at once: type a few words and watch them become the model\u2019s next word, passing through every stage the other pages cover. This is literally what happens each time you press enter, repeated once per word it writes back \u2014 a short pipeline, run over and over.",
  "gated-deltanet": "A newer \"linear attention\" memory used in hybrid models (Qwen3-Next): a compact memory that overwrites an old fact when its key comes back, instead of piling up forever. Part of the race to get attention-quality at SSM-like cost.",
  "gqa-mqa": "A memory-saving trick: rather than every attention head keeping its own running notes, several heads share one set. It is much of why modern models hold long conversations without needing far more GPU memory, and what it gives up is a little per-head detail.",
  "grpo-advantage": "How models are trained to reason, in the one part you can actually watch: the model tries a problem several times and each attempt is judged against the average of its siblings. If they all agree, there is nothing to learn from \u2014 which is a real problem trainers work around.",
  "guidance": "The dial that decides how literally an image generator follows your prompt. It works by running the model twice \u2014 with and without your words \u2014 and pushing away from the version that ignored you. Push too hard and everything you make starts to look the same.",
  "hybrid-by-layer": "The newest models (Jamba, Zamba, Qwen3-Next) mix the two: mostly cheap SSM layers with a few full-attention layers sprinkled in. The page shows where the expensive memory (the KV cache) actually lives \u2014 and why these hybrids need far less of it.",
  "hybrid-cache-allocator": "Newer models mix two kinds of layer, and their memory behaves in opposite ways \u2014 one grows with every word, the other is claimed in full the moment a conversation starts. A server holding both has to choose which limit to hit first.",
  "hyper-connections": "A change to the plumbing every transformer has used since 2015. Instead of one shared channel that each layer reads and adds to, there are several, and the model learns which to read and which to write \u2014 buying depth without the usual instability.",
  "kv-cache": "When a chatbot types its reply word by word, it saves its earlier work in a \"KV cache\" so it doesn't redo the whole conversation each step. This cache is why long chats eat more memory, why there's a \"context limit,\" and why your GPU fills up.",
  "kv-eviction": "Why a long conversation does not simply fall over when it outgrows the memory set aside for it. Keeping only the most recent part seems obvious and breaks the model; keeping the first few tokens as well, or keeping whatever the model actually kept looking at, works.",
  "kv-quant": "The trick that buys a longer conversation without a bigger machine. The running memory of a chat grows with every word, so storing it in fewer bits stretches how far the conversation can go \u2014 as long as the rounding does not lose the parts that mattered.",
  "kv-tiering": "Where a conversation goes when it no longer fits in the fastest memory. It moves down to ordinary memory and then to disk \u2014 and when it is needed again, fetching it back is not always quicker than just working it out from scratch.",
  "latent-space": "Why generating an image is affordable at all: the model never touches full-resolution pixels, only a compressed summary. It also explains a whole class of failure \u2014 fine detail and small text are often lost before generation even begins.",
  "lm-head": "The final step that scores every possible next word in the vocabulary. The page shows how those scores come out and the real-world tricks (tied weights, soft-capping) used to keep it cheap and stable.",
  "logit-lens": "Peeks at the model\u2019s guess at every layer instead of only the last, so you can watch an answer form with depth. The payoff is practical: it shows early layers doing grammar and late layers doing meaning, which is why tricks like early exit and layer skipping work at all.",
  "mamba-block": "The full Mamba layer wrapped around that running summary: a short convolution for local patterns, a gate deciding how much of each new word to admit, then the scan itself. It is the drop-in alternative to a transformer block \u2014 same job, a very different cost curve.",
  "matmul": "Multiplying grids of numbers is the operation a neural network is built from \u2014 every layer is one. It is the reason AI needs powerful GPUs: a model thinking is mostly doing trillions of these. Understand this one thing and \u201ccompute cost\u201d stops being a mystery.",
  "mla": "One of the reasons the newest large models can afford very long contexts. Rather than remembering everything for every attention head, they remember a compact summary per word and reconstruct the detail on the fly \u2014 much less memory, a little more arithmetic.",
  "mlp-gated": "After attention mixes words, each word passes through a small two-layer network where much of the model's knowledge lives \u2014 it's roughly two-thirds of all the parameters. The \"gated\" (SwiGLU) form is what modern models use.",
  "moe-balance": "For MoE to work, traffic must spread evenly across experts; otherwise a few overload (and drop tokens) while others never get trained and \"starve.\" The page shows that failure and the load-balancing fix \u2014 a big reason MoE models are finicky to train.",
  "moe-routing": "An MoE model has many sub-networks (\"experts\") but uses only a few per word \u2014 like an ER routing each patient to the right specialists. This lets a model be huge (lots of knowledge) yet cheap to run (only a slice active per token). It's how Mixtral, DeepSeek, and Qwen-MoE work.",
  "multi-head": "Models run several attentions side by side, called heads, each free to watch a different kind of relationship \u2014 grammar, meaning, position. It is how a single pass tracks many things about a sentence at once instead of being forced to pick one notion of relevance.",
  "multimodal-inject": "How images and audio get fed into a text model: encode them into the same kind of vectors as words and splice them into the sequence. It's the mechanism behind \"send the model a photo\" (GPT-4o, Claude, Gemini).",
  "normalization": "Keeps the numbers flowing through a deep network from exploding or fading away, so that training stays stable. It sits before every block in essentially every model ever shipped \u2014 unglamorous plumbing, and the difference between a network that trains and one that does not.",
  "paged-attention": "The reason a chat service can keep thousands of conversations alive at once. Reserving each conversation\u2019s worst case up front wastes most of it; handing out small fixed blocks on demand wastes almost none, and two chats that open identically can share blocks until they diverge.",
  "parallelism": "Why running a very large model takes several chips wired closely together, and why the wiring is often the real limit rather than the chips. There are a few ways to cut a model up, and they differ enormously in how much data must cross between the pieces for every word.",
  "pass-at-k": "An uncomfortable result about training models to reason. Given one attempt, the trained model wins. Given many attempts, the untrained original can win \u2014 because the training narrowed what it is willing to try rather than teaching it anything new.",
  "patch-embedding": "How a Vision Transformer turns an image into \"words\": chop it into patches and flatten each into a vector. This bridge is what let transformers (built for text) handle images \u2014 the basis of ViT, CLIP, and today's multimodal models.",
  "per-token-compute": "Why a model spends the same effort on the word \u201cthe\u201d as on a hard technical term, and what happens when you let it stop early instead. The saving is real, but ragged work suits GPUs badly, so it can cost more than it saves.",
  "pooling": "Shrinks an image map by keeping the strongest signal (max) or the average in each patch \u2014 cheaper compute plus a little \"don't care exactly where it was\" tolerance. The classic downsampling step in a CNN.",
  "prefill-vs-decode": "The two phases of running a model: reading your whole prompt at once (fast, \"prefill\") vs writing the answer one word at a time (slow, \"decode\"). It explains the pause-then-stream you see, and why decode is the expensive part to serve at scale.",
  "prefix-routing": "Why a service sometimes routes you to a particular machine rather than the least busy one: that machine already did the work of reading your prompt. Push the idea too far and everybody lands on one overloaded machine while the rest do nothing.",
  "qkv-projection": "Before attention, each word becomes three views: a Query (what am I looking for?), a Key (what do I offer?), and a Value (what do I carry?). This first step is how a transformer sets up words to find and pull information from each other.",
  "quantization": "Compressing a model's numbers to fewer bits (int8 / int4) so it's smaller and faster \u2014 how a 70-billion-parameter model squeezes onto a single GPU or a laptop. The page shows what accuracy you trade for the savings.",
  "radix-attention": "Why apps that send the same long instructions with every request are cheaper than they look. The server keeps prompts in a tree; anything a new request shares with an old one is already computed, so only the genuinely new tail costs anything.",
  "real-attention": "Real GPT-2 attention, computed in your browser. Type a sentence and look through all 144 heads for the ones researchers have named: the head that copies a repeated pattern, the one that looks a single word back, the one that parks on the first token. On real weights.",
  "real-embeddings": "Runs a real sentence-embedding model on words you type and draws the actual similarity map: king lands near queen, paris near tokyo. This is the machinery behind semantic search, \u201crelated articles\u201d, and the retrieval step in every RAG system \u2014 on real weights, not a toy.",
  "real-generate": "A real GPT-2 writing text in your browser, one token at a time, with the sampling knobs live. Watching the loop \u2014 predict, pick, append, repeat \u2014 is the fastest cure for thinking a model \"plans\" a sentence: it only ever chooses the next word.",
  "real-kv-cache": "Watch the model's short-term memory physically grow, one slot per word, as a real GPT-2 writes. It's the concrete version of \"why does a long conversation get slower and more expensive?\" \u2014 you can see the thing that is filling up.",
  "real-logits": "The real next-word distribution from GPT-2 for whatever prefix you type (\"the cat ran\" -> up / away / off / out), and what temperature, top-k and top-p really do to it. It turns the abstract \"creativity slider\" in every chat UI into something you can watch move.",
  "real-neurons": "Which individual neurons inside the model light up for which words. Some fire for a topic, some for a pattern of syntax, some for nothing you can name. This is the raw material of interpretability research \u2014 and why \u201cnobody fully knows what is inside\u201d is about scale, not mystery.",
  "real-quant": "Takes one real weight matrix out of GPT-2 and compresses it in front of you, so the accuracy you lose at 8 / 4 / 3 bits is measured on real weights rather than asserted. This is the exact decision behind \"will the 4-bit build be good enough on my laptop?\"",
  "real-vision": "A real Vision Transformer classifying a real photo, showing the image chopped into the 16x16 patches it treats as \"words\" plus its actual top-5 answers. It closes the loop from patch-embedding: the same transformer machinery, applied to pixels.",
  "receptive-field": "How much of the original image one deep neuron can \"see.\" Stacking tiny filters grows that view, so a deep network perceives the whole image even though each filter is small \u2014 the reason depth (and dilation, \u00e0 la WaveNet) matters.",
  "residual-block": "The skip connection that let vision networks jump from around twenty layers to over a thousand, by giving the learning signal a shortcut straight through each block. Transformers borrowed exactly the same trick, which is why it turns up in both halves of this collection.",
  "residual-stream": "The \"highway\" running the length of a transformer: each block reads from it, does a little work, and adds the result back instead of overwriting. This skip-connection idea is why we can train models hundreds of layers deep.",
  "ring-attention": "How a model reads a document far too long to fit on any one chip. The chips are arranged in a ring and pass pieces of the text around between them, each doing its share as a piece arrives, so no single machine ever has to hold the whole thing at once.",
  "rope": "How a model knows word order \u2014 \"dog bites man\" vs \"man bites dog.\" RoPE encodes each word's position by rotating its numbers; it's also the trick behind extending a model's context length past what it trained on.",
  "sampling": "How the model actually picks the next word from its probabilities \u2014 always the top one (deterministic) or rolling the dice (creative). Temperature / top-k / top-p are the knobs behind \"make it more creative\" vs \"make it more focused.\"",
  "scaled-dot-attention": "Attention lets each word gather meaning from the others \u2014 \u201cthe cat sat on it; what is it?\u201d Compare, weight, combine. This is the core mechanism inside every transformer, and every other attention page here is a variation on the formula shown on this one.",
  "softmax": "Softmax turns a list of raw scores into probabilities that add up to 100%. It's how a model goes from \"this word scored 8.3, that one 2.1\" to \"70% chance it's this word.\" Every classifier and every next-word prediction ends in a softmax.",
  "sparse-attention-select": "How a model with a very long memory decides what is worth paying attention to. Re-reading everything gets expensive, so a cheap scorer ranks the past and only the top slice is actually read. Fast \u2014 until the thing you needed falls just outside the cut.",
  "speculative-decoding": "Why some services answer noticeably faster without switching to a smaller model. A cheap model guesses the next few words, the real one checks them all in a single pass, and every guess it agrees with was effectively free. Guess badly and you have paid for nothing.",
  "ssm-scan": "An alternative to attention (Mamba) that keeps a small running summary of everything so far instead of a growing memory of every word. The payoff: cost stays flat as the text gets longer (attention's cost grows with the square), which is promising for very long inputs.",
  "suffix-decoding": "A way to make a model answer faster that costs nothing extra to run. It simply remembers what has already been said and guesses that a repeat is coming \u2014 brilliant on repetitive work, useless on genuinely new writing.",
  "thinking-tokens": "Why some models \u2018think before answering\u2019, and why that is not a different kind of model. The thinking is ordinary writing, fenced between two markers \u2014 so the length of it is a setting, and more of it stops helping sooner than you would expect.",
  "tokenization": "Models do not read letters; they read a fixed list of tokens, whole words or word-pieces. That list is built by repeatedly gluing together the commonest pair of characters. It is why prompts are billed per token, and why rare words get chopped into odd-looking chunks.",
  "transformer-block": "One full layer assembled end to end: normalise, attend, add, normalise, think, add. Stack several dozen identical blocks and you have a modern language model. The page shows how the pieces you have seen separately snap together into the unit that actually repeats.",
  "verifier-selection": "When a model tries a problem several times, something has to choose the answer. Picking whichever a scoring model likes best sounds obvious, and it gets worse the more attempts you allow, because you are really searching for what fools the scorer.",
  "video-time": "Why generating a few seconds of video costs so much more than an image. Time is treated as just another direction to chop up, and the numbers get out of hand quickly \u2014 a short clip can be worth hundreds of thousands of tokens.",
  "vlm-connector": "What happens when you attach an image to a message. The picture is chopped up and converted into the same kind of tokens as words, and the choice of how to convert it decides whether one photo costs a paragraph or an entire book of your context.",
  "vram-budget": "The arithmetic behind \u201cwill this model fit on my GPU?\u201d \u2014 the weights, plus the memory the conversation itself consumes, plus working space. It is the most practical question in running a model yourself, and the answer is rarely the number on the download page."
};
